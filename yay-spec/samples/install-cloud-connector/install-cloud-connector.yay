Do:
  - Configure Remote Runner in Release:
      releaseServer: ${releaseEndpoint}
    As: runner

  - Prepare Agent in Staging:
      host: ${releaseEndpoint.url}
      token: ${runner.token}
    As: agent

  - Download Cloud Connector Installer

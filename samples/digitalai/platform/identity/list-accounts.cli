Script info: Lists accounts in Digital.ai Platform

Input:
  name: Account name

Connect to: Digital.ai Platform API

---
GET: /identity/v1/accounts?sort=name&filter=name%5Bisearch%5D%3A${name}

For each:
  ${account} in: ${out.accounts}
  Out:
    name: ${account.name}
    vanity_domain: ${account.vanity_domain}
    id: ${account.id}
As: accounts

Print: ${accounts}

Script info:
  description: Lists accounts in Digital.ai Platform
  input:
    name: Account name

---
Connect to: Digital.ai Platform

GET: /identity/v1/accounts?sort=name&filter=name%5Bisearch%5D%3A${input.name}

For each:
  ${account} in: ${output.accounts}
  Output:
    name: ${account.name}
    vanity_domain: ${account.vanity_domain}
    id: ${account.id}
As: accounts

Print: ${accounts}

Script info: Lists the runtimes that are available for the agents

Connect to: Digital.ai Platform API

---
GET: /workload/v1/agent_runtimes/

For each:
  ${runtime} in: ${out.agent_runtimes}
  Out:
    name: ${runtime.name}
    description: ${runtime.description}
    id: ${runtime.id}

Print: ${out}
$schema: "https://instacli.dev/positive-json-schema/v1-beta"
type: array
items:
  - type: Condition with then
  - type: Else
    maxOccurs: 1

$types:
  Condition with then:
    type: Condition
    properties:
      then:
        type: object
        required: true
  Else:
    type: object
    properties:
      else:
        type: object
        required: true

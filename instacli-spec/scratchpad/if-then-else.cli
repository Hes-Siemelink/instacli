# Before:
${search}: ""
If:
  not:
    empty: ${input.name}
  then:
    ${search}: "&filter=name%5Bisearch%5D%3A${input.name}"

---
# After:
${search}:
  :If:
    empty: ${input.name}
    then:
      Output: ""
    else:
      Output: "&filter=name%5Bisearch%5D%3A${input.name}"

---
# Alternative:
If:
  empty: ${input.name}
  then:
    ${search}: ""
  else:
    ${search}: "&filter=name%5Bisearch%5D%3A${input.name}"

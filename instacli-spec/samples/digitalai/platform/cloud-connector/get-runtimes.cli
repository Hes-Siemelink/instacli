Script info:
  description: Lists the available runtimes for use in the agents

---
Get token:
  endpoint: ${digitalaiStaging}
Http endpoint: ${output}

---
Http GET: /workload/v1/agent_runtimes/

For each:
  ${runtime}: ${output.agent_runtimes}
  Output:
    name: ${runtime.name}
    description: ${runtime.description}
    id: ${runtime.id}

Print as YAML: ${output}
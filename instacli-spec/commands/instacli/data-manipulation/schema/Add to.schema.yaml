$schema: https://json-schema.org/draft/2020-12/schema

type: object
patternProperties:
  "^\\$\\{([^}]+)}$": # Variable syntax. For example: ${var}
    type:
      - string
      - number
      - array
      - object
additionalProperties: false

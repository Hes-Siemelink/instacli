$schema: https://json-schema.org/draft/2020-12/schema

type: object

properties:
  text:
    type: string
  in:
    type:
      - string
      - array
      - object
  replace with:
    type: string

required:
  - text
  - in
  - replace with
additionalProperties: false
